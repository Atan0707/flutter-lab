import 'package:flutter/material.dart';

class StaffLandingPage extends StatelessWidget {
  const StaffLandingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Staff Landing Page'),
      ),
    );
  }
} 